//just type _rafce, then enter. Command + Shift + R doesn't work, for some reason

const Header = () => {
  return (
    <header>
      <h1>Groceries List</h1>
    </header>
  );
};

export default Header;
